
        // Lambda autoejecutable: expresión que devuelve un lambda y
        // éste se ejecuta en el mismo instante

        (() => {

        })();

        // Función autoejecutabl: expresión que devuelve una función
        // y esta se ejecuta en el mismo instante

        (function () {

        })();

        // Funciones con nombre

        function nombre () {

        }

        nombre();

        // Funciones como variables

        var funcion = function () {

        };

        funcion();
